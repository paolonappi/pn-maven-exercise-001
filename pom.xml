<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	
	<!--The group ID in Maven is typically based on the reverse domain name of the organization or individual that owns the project. -->
	
	<groupId>com.pn.exercises</groupId>

	<!--
	The the artifact ID in Maven is a unique identifier for a project or module within a group, and it plays a crucial role in dependency management and project identification.
	--->

	<artifactId>pn-hello-maven</artifactId>

<!-- 
When a project has multiple versions, Maven uses the version information to resolve dependencies.
Version of a Maven project is separate from the artifact ID and group ID.
The combination of these three coordinates (group ID, artifact ID, and version) helps to uniquely identify and locate the specific version of a project or module. -->

	<version>1.0.0</version>
	
	<name>Hello Maven</name>
	
	<description>Hello Maven</description>

	<properties>

		<!--
		<java.version>XXX</java.version>	
		
		<maven.compiler.source>YYY</maven.compiler.source>
		
		<maven.compiler.target>ZZZ</maven.compiler.target>	
		-->
		
	</properties>

	<dependencies>

	</dependencies>

	<build>

	</build>
	
	<profiles>

	</profiles>

</project>
